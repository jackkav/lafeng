schema {
  query: Query
  mutation: Mutation
}

type Query {
  visitor: Visitor
  visitors: [Visitor]
  user(nickname: String!): User
  users: [User]
}

type Mutation {
  createUser(nickname: String!, mobile: String): Boolean!
}
